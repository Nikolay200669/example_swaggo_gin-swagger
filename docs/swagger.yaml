basePath: /
definitions:
  main.Resp:
    properties:
      name:
        type: string
    type: object
info:
  contact: {}
paths:
  /ping:
    get:
      consumes:
      - application/json
      description: do ping
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/main.Resp'
      summary: ping example
      tags:
      - example
swagger: "2.0"
